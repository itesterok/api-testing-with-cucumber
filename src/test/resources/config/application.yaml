spring:
  main:
    banner-mode: off
app:
  base-url: https://api.apilayer.com
  fixer-api-key: ${FIXER_API_KEY}
  number-verification-api-key: ${NUMBER_VERIFICATION_API_KEY}
  fixer-api-key-expired: ${FIXER_API_KEY_EXPIRED}
